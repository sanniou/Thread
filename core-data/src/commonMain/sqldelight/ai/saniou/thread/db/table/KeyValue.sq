CREATE TABLE KeyValue (
    key TEXT NOT NULL PRIMARY KEY,
    content TEXT NOT NULL
);

insertKeyValue:
INSERT OR REPLACE INTO KeyValue(key, content) VALUES (?, ?);

getKeyValue:
SELECT * FROM KeyValue WHERE key = ?;

deleteKeyValue:
DELETE FROM KeyValue WHERE key = ?;